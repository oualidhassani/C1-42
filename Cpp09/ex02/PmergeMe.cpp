#include "PmergeMe.hpp"
void Step1(std::vector<int> &vec)
{
    int level = 1;

    while((1 << level) * 2 >= vec.size())
    {
        int levgroup = level << 1;
        int grp_size = levgroup * 2;
        int pair_size = 

    }
}
void PmergeMe::sort(std::vector<int> &vec)
{


}